{
  "name": "@dither-kit/registry-core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared schema for the Dither Kit registry: the single source of truth for the registry-item shape and the CLI lockfile, as zod schemas with inferred types. Consumed from source (bundled into @dither-kit/cli); never published or built on its own.",
  "homepage": "https://tripwire.sh/dither-kit",
  "repository": {
    "type": "git",
    "url": "https://github.com/Boring-Software-Inc/dither-kit.git",
    "directory": "packages/registry-core"
  },
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
